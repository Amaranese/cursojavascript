  /*
  Crea dos objetos, que serán dos personas. Cada uno debe tener las propiedades
  Las propiedades `name` y `lastName`, rellenadas con el nombre y los apellidos de la persona.
  */
  // ?

  /*
  Ahora cree una función llamada `getFullName` que devuelva el
  `nombre` y `apellido` de los objetos anteriores, haciendo un nombre completo.
  La función no debe tomar ningún parámetro, pero el `nombre` y
  `apellido` debe ser dinámico.
  La misma función debería funcionar para ambas personas (o cualquier otra cosa que
  creado).
  Después de esto, invoca esta función, mostrando a la consola los nombres completos de los
  personas que creó anteriormente, pasando las personas anteriores como el
  contexto de la función. Utilice un console.log por persona.
  */
  console.log( 'El nombre de la persona es:' );
  // ?

  /*
  Crea una función llamada `suma`. Esta función puede tomar una lista variable de
  parámetros de la variable, y debe devolver la suma de todos ellos.
  No utilices estructuras de bucle para sumar los argumentos.
  En la primera línea, dentro de la función, deja un console.log para mostrar todo
  los parámetros pasados a esta función.
  */
  // ?

  /*
  Muestra en la consola que la función anterior funciona, invocándola en 3 console.log diferentes
  con una cantidad variable de parámetros que se le pasan.
  */
  console.log( '\N- Guardar algunos números:' );
  // ?

  /*
  Declarar una variable llamada `userEntry` que recibirá algunos valores
  introducido por el usuario. Muestre al usuario la siguiente frase:
  "Introduzca algunos números que se sumarán:"
  */
  // ?

  /*
  Muestra el valor introducido por el usuario en la consola:
  */
  console.log( '\NIntroducción del usuario:' );
  // ?

  /*
  Cree una función llamada `justNumbers` que tome una cadena como parámetro
  y elimina todo lo que no es un número, devolviendo una matriz con sólo los números
  de la cadena. Muestra la representación de la cadena de esta función en la consola.
  */
  console.log( '\NFunción que borra la entrada del usuario (sólo números):' );
  // ?

  /*
  Utilizando la función anterior, limpie los valores introducidos por el usuario,
  asignando el resultado a una variable "números".
  */
  console.log( '\NLa entrada del usuario se ha borrado. Sólo números:' );
  // ?

  /*
  Ahora con el array de números, utiliza la función `sum` para sumar todos los
  suma todos los números de esa matriz y muestra el resultado en la consola.
  */
  console.log( '\NSuma de números introducidos por el usuario:' );
  // ?
